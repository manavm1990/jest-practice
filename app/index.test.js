// TODO: `import` all other fxns. from `index.js`
// Write the tests ✅.
// DON'T COPY/PASTE - time on task 👩🏾‍💻 ⌨️.
import { add } from "./index.js";

// TODO: Add `describe` as appropriate to group the similar types of tests.
it("adds 1 + 2 to equal 3", () => {
  expect(add(1, 2)).toBe(3);
});
